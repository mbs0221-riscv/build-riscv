# git

## git config

```
$ git config --global user.name "benshan mei"
$ git config --global user.email "mbs2021@163.com"
$ git config --global http.proxy http://$IPADDR:8889
$ git config --global https.proxy https://$IPADDR:8889
$ git config -l
```
